---
layout: "home"
---

%section.outer-container
  %section.article-directory
    - blog.articles.each do |article|
      %article.article-item
        %h2.article-title= article.title
        %small.article-date= format_date(article.date)
        .article-body= article.body
