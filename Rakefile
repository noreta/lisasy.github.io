desc "Create view files for corresponding projects from data file"
task :generate_new_project_view do
  # Look at the first item in the project array.
  # Look at the slug title and save it.
  # Check for the /projects/ folder and find if there is a file containing that slug title.
  # If exists, do nothing, else, create a new file in that folder, with that slug.
  # Go to the next item in the project array.
end
